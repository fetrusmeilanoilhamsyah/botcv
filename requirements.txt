# Core bot framework — [job-queue] WAJIB agar app.job_queue tidak None
python-telegram-bot[job-queue]>=21.0

# Database
# sqlalchemy dihapus — bot pakai sqlite3 native, tidak perlu SQLAlchemy

# Utils
python-dotenv>=1.0.0
openpyxl>=3.1.2

# HTTP client (Pakasir API)
httpx>=0.27.0

# Webhook server (health check + pakasir webhook)
aiohttp>=3.9.1

# QR code generator (opsional — jika tidak ada, fallback ke teks)
segno>=1.6.0